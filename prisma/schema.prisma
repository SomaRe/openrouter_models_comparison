generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Comment {
  id       String @id @default(cuid())
  modelId  String @unique
  comments String

  @@map("comments")
}
