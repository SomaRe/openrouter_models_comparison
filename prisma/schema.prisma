generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Comment {
  id       String @id @default(cuid())
  comments String

  @@map("comments")
}
